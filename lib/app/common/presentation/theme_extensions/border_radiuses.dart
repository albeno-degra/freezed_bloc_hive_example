import 'package:flutter/material.dart';

extension BorderRadiusesExtension on BuildContext {
  BorderRadius get borderRadius16 =>
      const BorderRadius.all(Radius.circular(16));
  BorderRadius get borderRadius8 => const BorderRadius.all(Radius.circular(8));
}
